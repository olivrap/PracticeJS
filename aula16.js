//variáveis mutáveis e imutáveis
// abaixo temos exemplos de variáveis imutáveis
const numero = 134132542;
const str = "Minha primeira string";
const bool = true;

// array são variáveis mutáveis, veja o exemplo com o push
const arrayTest= [1955, 1957 , 1986];
arrayTest.push(1987);

console.log(arrayTest);